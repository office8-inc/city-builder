# 🚂 A列車で行こう風 都市開発鉄道シミュレーション — ゲームデザインドキュメント

## 1. ゲームコンセプト

### 1.1 概要
プレイヤーは**鉄道会社の社長**として、線路を敷き、列車を走らせ、街を発展させる「都市開発鉄道シミュレーションゲーム」。

A列車で行こうシリーズ（特にA9/はじまる観光計画）を参考に、**鉄道運営が都市発展の核心**となるゲーム性を実現する。SimCity系の「上から建物を配置する」ゲームではなく、**鉄道インフラの整備によって街が自律的に発展する**点が最大の特徴。

### 1.2 コアループ
```
線路を敷く → 駅を建てる → 列車を走らせる
                ↓
        駅周辺に人が集まる → 建物が自動的に建つ
                ↓
        人口増加 → 税収増加 → さらに投資
                ↓
        新路線の建設 → 都市圏の拡大
```

### 1.3 SimCity型との根本的な違い

| SimCity型 | 本作（A列車型） |
|---|---|
| プレイヤーが建物を直接配置 | 建物は**駅周辺に自動発展** |
| 道路がインフラの中心 | **鉄道**がインフラの中心 |
| ゾーニング（R/C/I） | 産業誘致 + 自動発展 |
| 静的なグリッド | **列車が実際に走る**動的システム |
| 時間はシンプルな進行 | 昼夜サイクル + ダイヤ運行 |

---

## 2. コアシステム設計

### 2.1 マップシステム
- **マップサイズ**: 128×128タイル
- **高度**: `height`（地上の起伏、0〜） + `elevation`（線路の地下/地上/高架レイヤー、後述）
- **地形タイプ**: 平地(flat)、丘陵(hill)、山岳(mountain)、水域(water)、森林(forest)
- **地形生成**: Simplex Noiseによるプロシージャル生成（シード指定可、`?seed=` で共有可能）
  - v1.0: マップ中央部は平野を保証する**動径バイアス**を導入し、中心から離れるほど丘陵・山岳・水域が出現しやすくする（初期プレイエリアの平坦性を確保するため）

### 2.2 鉄道システム（最重要）

#### 2.2.1 線路
- **線路**: 8方向（N/NE/E/SE/S/SW/W/NW）の直線セグメントのみ。曲線レール・分岐器（ポイント）の専用ツールは持たない
  - 同一タイルに複数方向の線路セグメントを重ねて敷設することで、見た目上の曲線・分岐点が自然に形成される
  - 分岐点での進路は列車ごとの**ダイヤの次駅方向**に基づいて自動的にルーティングされる（`trackUtils.ts`）。プレイヤーがポイントを手動で切り替える操作はない
  - 斜め方向の敷設は直線の1.5倍のコスト、長さは√2倍
- **高架線路**: `elevation` を正の値にして地上構造物の上を通過。高架柱（Kenney Props `supports_high`）を自動表示
- **地下線路（v1.0）**: `elevation = -1` のトンネル区間
  - 建設費は地上の**3倍**
  - 建物・道路のあるタイル、および**山岳タイル**にも敷設可能（トンネルとして地下を貫通）。水域タイルには敷設不可
  - 3D表現: 半透明の暗色線路として描画。地下区間を走行中の列車も半透明表示になる
  - 山岳地形を横断する唯一の手段は地下線路（トンネル）であり、地上線路は山岳タイルを通過できない
- **信号（実装済み）**: ブロック信号システム（`Signal`）。赤/黄/緑の3灯で、赤信号区間では列車が自動停止する

#### 2.2.2 駅
| 駅タイプ | 説明 | ホーム数 |
|---|---|---|
| 地上駅（小） | 田舎・ローカル線向け | 1〜2 |
| 地上駅（大） | 都市部の主要駅 | 2〜4 |
| 高架駅 | 高架線路に設置 | 1〜3 |
| 始発駅 | 頭端式ホーム（ターミナル） | 2〜6 |
| 地下鉄駅 | 地下に建設。地上には入口構造物のみ表示される | 1〜4 |
| 操車場 | 車両の留置・整備 | 1〜4 |

- 駅周辺に**乗降客が発生**し、`dailyPassengers` / `activityLevel` として管理される

#### 2.2.3 列車（車両）
- **車両タイプ（実装済み7種）**:
  - 通勤型電車（local）
  - 近郊型電車（suburban）
  - 急行型電車（express）
  - 気動車（diesel、非電化路線向け）
  - 貨物列車（freight、資材運搬）
  - 新幹線（shinkansen、超高速）
  - 蒸気機関車（steam、観光・レトロ）
- **編成**: 車両タイプごとに固定の両数・最高速度・定員を持つ
- **購入・維持費**: 車両によって異なる

#### 2.2.4 ダイヤ設定
- 各列車に`TrainSchedule`（停車駅リスト・現在停車インデックス・運行パターン）を設定
- 駅ごとの操作: 停車(stop) / 通過(pass)、待機時間、発車時刻（任意）
- **運行パターン（v1.0で走行ロジックへ完全反映）**:
  - **循環（loop）**: 終端駅に到達後、先頭駅へ向かって運行を継続する（環状運転）
  - **往復（bounce）**: 終端駅で折り返す（既存の標準動作）
  - **片道（one-way）**: 終端駅に到達すると運行終了状態（`state: 'stopped'`）になり停止。ダイヤ設定パネルから手動で再出発させるまで停車したまま
- **信号連動**: 先行列車や赤信号に接近すると自動減速・停止する

### 2.3 都市発展システム

#### 2.3.1 自動発展の仕組み
1. 駅を建設すると**影響範囲**（`influenceRadius`）が発生
2. 列車が走ると乗降客が生まれ、**駅の活性度**（`activityLevel`）が上昇（距離減衰あり）
3. 活性度に応じて駅周辺に**建物が自動的に建設**される
4. 建物の種類は**産業相性**（`SYNERGY_MATRIX`）と乗降客数で決まる
5. 時間経過で建物がレベルアップ（レベル1〜3は近隣状況のみで進行）
6. **レベル4以上へのレベルアップは近隣の資材ストックを消費することが必須**（資材が無いと頭打ちになる）

#### 2.3.2 産業区分（7種類）
| 産業 | 代表的な建物 | 特徴 |
|---|---|---|
| 住宅 | 一戸建て、アパート、マンション、タワマン | 人口の源泉 |
| 商業 | コンビニ、商店街、デパート、ショッピングモール | 消費活動 |
| オフィス | オフィスビル、超高層ビル | 雇用の源泉 |
| 工業 | 工場、倉庫、プラント | 資材生産、雇用 |
| 農業 | 田畑、農家、サイロ | 田園風景 |
| レジャー | 遊園地、ホテル、温泉、ゴルフ場 | 観光客誘引 |
| 文化 | 学校、図書館、美術館、神社仏閣 | 地価向上 |

#### 2.3.3 産業間の相性
- 住宅 ↔ 商業: **相乗効果**（互いに発展を促進）
- 住宅 ↔ 工業: **やや反発**（公害で地価低下、でも雇用は生む）
- 商業 ↔ オフィス: **相乗効果**（ビジネス街形成）
- レジャー ↔ 文化: **相乗効果**（観光地化）
- 工業 ↔ レジャー: **反発**（工業地帯に観光客は来ない）

#### 2.3.4 資材システム（v1.0で輸送フローを完成させる）
- **工場・資材工場**が資材を生産し、自タイルの`materialStock`に蓄積する（実装済み）
- **v1.0新規**: **貨物列車**が近隣駅で資材を積載（`materialLoad`）→別の駅まで走行→荷降ろし（荷降ろし時に輸送収入を発生させる）→駅周辺タイルの`materialStock`が増加
- 駅周辺の資材ストックは`getMaterialStockNear()`で集計され、**建物のレベル4以上へのレベルアップ条件**として消費される
- 資材が枯渇している地域は大型建築へ発展できない

### 2.4 経営システム

#### 2.4.1 収入源
- **運賃収入**: 乗客数 × 距離 × 運賃率（車両タイプごとのプレミアム倍率あり）
- **子会社利益**: 自社で建設した施設の収益
- **株式配当**（保有株の配当金は将来検討）
- **不動産収入**: 所有する土地（`ownedLand`）からの地代収入

#### 2.4.2 支出
- **車両購入費・維持費**
- **線路・駅の建設費・維持費**
- **子会社建設費・運営費**
- **人件費**
- **借入金利子**

#### 2.4.3 資金管理
- 初期資金: シナリオ・モードにより変動
- **銀行借入（実装済み・4段階）**: 1億円(60ヶ月) / 5億円(120ヶ月) / 10億円(120ヶ月) / 50億円(180ヶ月)、年利4%固定、元利均等返済
- **経営破綻判定**: 資金がマイナスかつ負債が「総資産 or 初期資金 × `MAX_DEBT_RATIO`(3倍)」の上限を超えた場合に発生
- **緊急支援（v1.0新仕様）**: 経営破綻は1回限り「緊急支援」で回避可能。資金+2億円を得る代わりに、高利の救済融資が負債に追加され再建を試みられる。2回目の破綻判定でゲームオーバー確定
- 決算: 四半期ごとの収支記録（`quarterlyHistory`）

#### 2.4.4 子会社（実装済み15種類）
プレイヤーが**能動的に建設できる施設**（自動発展とは別）:
factory / depot / hotel / department_store / power_plant / material_yard / warehouse / resort_hotel / convenience_store / supermarket / office_building / apartment / amusement_park / stadium / broadcast_tower

### 2.5 道路・バスシステム
- **道路**: 自動発展により駅周辺・建物周辺に自動生成される（`materials.ts`の地価計算・接続性アルゴリズムでKayKit Cityの straight/corner/tsplit/junction を自動選択）
- **バス・トラック路線シミュレーション**: 未実装。将来検討の拡張候補（本ドキュメントの必須スコープには含まない）

### 2.6 時間・季節・天候システム
- **1日サイクル**: 昼夜が切り替わる（Skyコンポーネントが太陽位置に連動）
- **時間拡張**: 1分単位の時刻管理（`GameTime`）
- **速度制御**: 一時停止, ×0.5, ×1, ×2, ×4, ×8, ×16（`GameSpeed`）
- **季節（実装済み）**: 春夏秋冬（`Season`）を月から自動判定。地形カラー・植生が季節に応じて変化（春=桜、夏=緑、秋=紅葉、冬=雪）
- **天候（実装済み）**: 晴(clear)/曇(cloudy)/雨(rain)（`WeatherType`）が日次でランダム遷移。雨天時は2000粒子のInstancedMeshによる雨エフェクトと雲エフェクトを`Weather.tsx`で描画
- **時間帯による乗客倍率（実装済み・ラッシュアワー）**:
  - 朝ラッシュ（7:00-9:00）: ×2.5
  - 昼間（12:00-13:00）: ×1.3
  - 夕ラッシュ（17:00-20:00）: ×2.2

### 2.7 ビューシステム
- **フリーカメラ**: OrbitControlsによる俯瞰3Dビュー（右ドラッグ回転、ホイールズーム）
- **追尾カメラ**: chase（後方追尾）/ cab（運転台視点、車窓モード相当）。Vキーで切替
- **クォータービュー**: 固定ピッチ30°の古典的A列車ビュー。Q/Eで45°回転、WASDで移動
- **ミニマップ**: 全体を俯瞰する2Dマップ（右下常時表示）

---

## 3. ビジュアル設計

### 3.1 全体方針
外部3Dモデルは使用せず全てコード内でプロシージャル生成する、という当初方針は**採用していない**。現行実装は **Kenney City Kit / KayKit の外部GLB/GLTFアセットを建物・駅・列車・線路・道路・信号・子会社の全カテゴリに採用**している。

- **建物**: Kenney Suburban（住宅） / Kenney Commercial（商業） / Kenney Buildings（工業・文化） + KayKit-City（レジャー）
- **駅**: Kenney Buildings（platform_small / platform_long / platform_high, structure, hangar_largeA）
- **列車**: Kenney Train Kit 全7種（electric-city / electric-square / electric-double, diesel, bullet, locomotive, freight）
- **線路**: Kenney Train Kit（railroad-straight, rail-straight）
- **道路**: KayKit City（接続性アルゴリズムで straight / corner / tsplit / junction を自動選択）
- **子会社**: Kenney Buildings + KayKit-City（全15種）
- **信号**: KayKit City trafficlight
- **高架柱**: Kenney Props supports_high
- **森林**: Kenney Nature（detail_forestA）

各GLBは外部テクスチャ `Textures/colormap.png` を参照するため、モデルディレクトリごとにテクスチャを同梱・git追跡している。InstancedMeshで大量オブジェクト（森林・道路等）の描画負荷を抑える。

### 3.2 建物のビジュアル
| カテゴリ | レベル1 | レベル2 | レベル3 | レベル4+ |
|---|---|---|---|---|
| 住宅 | 一戸建て系モデル | アパート系モデル | 中層マンション系モデル | タワーマンション系モデル |
| 商業 | 小店舗モデル | スーパー/モールモデル | デパートモデル | 大型商業施設モデル |
| オフィス | 低層オフィスモデル | 中層ビルモデル | 高層ビルモデル | 超高層ビルモデル |
| 工業 | 小工場モデル | 中規模工場モデル | 大規模プラントモデル | - |
| レジャー/文化 | 施設モデル各種（KayKit-City） | - | - | - |

レベル4以上への到達には資材ストック消費が必須（2.3.4節）。

### 3.3 列車のビジュアル
- Kenney Train Kitの車種別GLBモデルを使用し、各車両タイプごとにシルエットが異なる
- 地下区間走行中は半透明表示（トンネル演出）

### 3.4 環境表現
- **Sky**: 太陽位置連動の空（Scene.tsx）
- **昼夜サイクル**: 朝焼け→昼→夕焼け→夜
- **水面**: シェーダーによる波・反射（Terrain.tsx）
- **季節**: 春（桜）、夏（緑）、秋（紅葉）、冬（雪） — 実装済み
- **天候**: 晴れ/曇り/雨 — 実装済み（雨粒子・雲エフェクト、2.6節参照）
- **フォグ**: 奥行き感の表現

### 3.5 UI設計
- **ガラスモーフィズム**: 半透明ブラー効果のパネル
- **ミニマップ**: 右下に常時表示
- **情報パネル**: 選択した駅/列車/建物の詳細情報
- **ツールバー**: 左側にカテゴリ別ツール（v1.0で土地売却ツールを追加）
- **タイムライン**: 下部に時刻・速度コントロール
- **財務サマリー**: 上部に資金・人口・日付
- **ダイヤ設定パネル（v1.0で導線整備）**: Gキー / HUDボタンから開く
- **設定パネル（v1.0で導線整備）**: Oキー / HUDボタンから開く（音量、ショートカット一覧）
- **確認ダイアログ（v1.0新規）**: 撤去・ロード等の破壊的操作の前に確認を挟む
- **マイルストーン通知（v1.0新規）**: 人口・資金・駅数などの節目達成時に一度だけ表示される軽量実績通知

---

## 4. 開発ロードマップ

### Phase 1: 基盤システム（v0.2） — 完了
**目標: 「線路を敷いて列車が走る」が動く**

- [x] マップシステム（128×128、地形生成）
- [x] 線路敷設システム（8方向直線、重ね敷きによる分岐形成）
- [x] 駅の建設（6種類）
- [x] 列車の配置と自動運行
- [x] 基本カメラ（俯瞰・回転・ズーム）
- [x] 昼夜サイクル
- [x] 基本UI（ツールバー、情報パネル）

### Phase 2: 都市発展（v0.3） — 完了
**目標: 「駅周辺に街が育つ」を実現**

- [x] 自動発展システム（駅の影響範囲で建物生成）
- [x] 産業区分（7種類）と建物バリエーション
- [x] 建物のレベルアップ（時間経過で成長）
- [x] 資材システム基礎（工場での生産・ストック）
- [x] 道路の自動生成
- [x] 乗降客のシミュレーション
- [x] 人口・地価の計算

### Phase 3: 経営（v0.4） — 完了
**目標: 「経営判断が楽しい」ゲーム性**

- [x] 収支システム（運賃・子会社）
- [x] 子会社の建設（15種類）
- [x] 銀行借入（4段階）
- [x] 四半期決算
- [x] 乗客数に基づく運賃計算
- [x] 土地の売買

### Phase 4: 高度な鉄道運営（v0.5） — 完了
**目標: 「ダイヤ設定が奥深い」鉄道シム**

- [x] ダイヤ設定UI
- [x] 複数路線の運行
- [x] 高架・地下鉄（駅タイプとして）
- [x] 複数車両タイプ（7種）
- [x] 信号システム（ブロック信号）

### Phase 5: ビューと演出（v0.6） — 完了
**目標: 「眺めているだけで楽しい」街**

- [x] 車窓モード（cab視点）
- [x] クォータービュー
- [x] 季節変化
- [x] 天候システム（雨粒子・雲）
- [ ] 環境音（列車音、街の音） — v1.0スコープ
- [ ] BGM — 将来検討

### Phase 6: シナリオ・マップ（v1.0進行中）
**目標: リプレイ性のある完成形**

- [x] プリセットシナリオ（3本、目標付きマップ）
- [x] コンストラクションモード（自由建設）
- [x] マップエディタ
- [x] セーブ/ロード（3スロット）
- [x] チュートリアル
- [ ] シナリオの成否判定（下記v1.0スコープ）

---

### v1.0完成スコープ（2026-07）

Phase 6の残タスクとして、以下を実装する。詳細仕様は各節を参照。

1. **線路の再設計**（2.2.1節）: 曲線・分岐器の専用ツールを廃止し、8方向線路の重ね敷きで分岐点を自然形成。列車はダイヤの次駅方向へ自動ルーティング
2. **地下線路**（2.2.1節）: `elevation=-1`。建設費3倍。建物・道路・山岳タイルに敷設可（トンネル）、水域は不可。半透明表現。地下鉄駅は地上に入口構造物を表示
3. **ダイヤ運行パターンの完全実装**（2.2.4節）: 循環(loop) / 往復(bounce) / 片道(one-way) の3種を走行ロジックへ反映
4. **シナリオの成否判定**（新規）: `timeLimit`年を超過し目標未達成なら「シナリオ失敗」画面。全目標達成で「シナリオクリア」画面（統計表示、「続けて遊ぶ」「タイトルへ」の選択）
5. **緊急支援システム**（2.4.3節）: 経営破綻を1回だけ緊急支援で回避可能。2回目はゲームオーバー確定
6. **資材輸送フローの完成**（2.3.4節）: 貨物列車の積載・荷降ろし・輸送収入・レベル4以上のレベルアップ条件消費までを一貫実装 — 実装済み
7. **地形生成の動径バイアス**（2.1節）: マップ中央部の平野保証。線路・建物は平地のみ設置可、山岳は地下線路（トンネル）でのみ貫通可能。森林は線路敷設時に伐採
8. **サウンド接続**: 実装済みの`utils/audio.ts`（Web Audio APIエンジン）に、建設音・エラー音・通知音・列車走行音（追尾/運転台視点時）・環境音を接続
9. **UI導線整備**: ダイヤ設定パネル（Gキー/HUDボタン）・設定パネル（Oキー/HUDボタン）の導線新設。土地売却ツールをツールバーへ追加。撤去・ロード等の破壊的操作に確認ダイアログを追加
10. **マイルストーン通知**: 人口・資金・駅数などの節目達成で一度だけ通知される軽量実績システム

---

## 5. 技術スタック

### 5.1 フロントエンド
- **Three.js** via **@react-three/fiber** — 3Dレンダリング
- **React 18** + **TypeScript** — UIとアプリケーション構造
- **Vite** — ビルドツール
- **Zustand** — ゲーム状態管理
- **@react-three/drei** — Three.jsヘルパー
- **@react-three/postprocessing** — ポストプロセッシング
- **Tailwind CSS** — UIスタイリング
- **simplex-noise** — 地形生成

### 5.2 パフォーマンス戦略
- **InstancedMesh**: 同種オブジェクト（森林・道路・大量建物等）の一括描画
- **チャンク分割・LOD**: 将来的な最適化候補（マップ規模が128×128の現状は未導入）
- **金額の整数管理**: 浮動小数点誤差を回避するため金額は常に整数（円単位）で扱う

### 5.3 データ構造
実装済みの主要な型（`src/game/types.ts`）を示す。

```typescript
// マップタイル
interface MapTile {
  terrain: TerrainType;       // 'flat' | 'hill' | 'mountain' | 'water' | 'forest'
  height: number;
  trackIds: string[];         // 同一タイルに複数線路セグメントが重なりうる
  buildingId: string | null;
  stationId: string | null;
  subsidiaryId: string | null;
  landValue: number;
  materialStock: number;
  roadLevel: number;
}

// 線路セグメント（8方向 + 高度レイヤー）
interface TrackSegment {
  id: string;
  startX: number; startZ: number;
  endX: number; endZ: number;
  type: 'straight';            // 曲線・分岐器の専用タイプは廃止済み（8方向線路の重ね敷きで表現）
  direction: Direction;        // 8方向
  elevation: number;           // -1=地下, 0=地上, 1+=高架
}

// 列車
interface Train {
  id: string;
  type: TrainVehicleType;      // 7種
  cars: number;
  currentSegmentId: string;
  positionOnSegment: number;   // 0-1
  speed: number;
  direction: 1 | -1;
  passengers: number;
  capacity: number;
  schedule: TrainSchedule;
  state: 'running' | 'stopped' | 'waiting';
  materialLoad: number;        // 貨物列車の積載資材量
}

interface TrainSchedule {
  stops: ScheduleStop[];
  currentStopIndex: number;
  loopMode: 'loop' | 'bounce' | 'one-way';  // 循環/往復/片道
}

// 駅
interface Station {
  id: string;
  name: string;
  type: StationType;           // 6種
  platforms: number;
  platformLength: number;
  dailyPassengers: number;
  influenceRadius: number;
  activityLevel: number;
}

// ブロック信号
interface Signal {
  id: string;
  x: number; z: number;
  segmentId: string;
  state: 'green' | 'yellow' | 'red';
}

// 経営データ
interface Finance {
  cash: number;
  debt: number;
  quarterlyIncome: IncomeBreakdown;
  quarterlyExpenses: ExpenseBreakdown;
  stockPrice: number;
  totalAssets: number;
}

// 融資（4段階）
interface Loan {
  id: string;
  principal: number;
  interestRate: number;
  monthlyPayment: number;
  remainingMonths: number;
  takenAtYear: number;
  takenAtMonth: number;
}

// 季節・天候
type Season = 'spring' | 'summer' | 'autumn' | 'winter';
type WeatherType = 'clear' | 'cloudy' | 'rain';

// シナリオ
interface ScenarioObjective {
  id: string;
  description: string;
  type: 'population' | 'income' | 'stations' | 'tracks' | 'trains' | 'cash';
  target: number;
  completed: boolean;
}

interface Scenario {
  id: string;
  name: string;
  description: string;
  difficulty: 'easy' | 'medium' | 'hard';
  mapSeed: number;
  initialCash: number;
  objectives: ScenarioObjective[];
  timeLimit?: number;  // 年単位。超過時はシナリオ失敗判定（v1.0）
}
```

### 5.4 URLパラメータ共有（実装済み）
- `?seed=数値` — マップシード指定（`TitleScreen.tsx`で読み取り`resetForNewGame(seed)`）
- `?scenario=ID` — シナリオ直接開始
- `?autoplay` — タイトル画面をスキップし直接ゲーム画面へ（スクリーンショットテスト用途）

### 5.5 セーブ/ロード
`src/game/saveLoad.ts` にてローカルストレージ3スロットで管理。バージョン管理付きで、新フィールド追加時はロード時にデフォルト値補完する後方互換方針を取る。

---

## 6. A列車で行こうから学ぶ重要ポイント

### 6.1 ゲームの本質
> **「鉄道を中心とした都市の発展」に重点を置く**

プレイヤーがやることは:
1. 発展が見込めそうな地域に**線路を敷く**
2. **駅を建てる**
3. **列車を走らせる**
4. 人の流れが生まれ、**街が自然に発展**する
5. 収益を元に**さらに路線を拡張**

### 6.2 A列車の魅力の核
1. **作った街を眺める楽しさ**（車窓モード、追跡モード）
2. **ダイヤ設定の奥深さ**（通勤ラッシュ対応、循環/往復/片道の使い分け）
3. **街が有機的に成長する驚き**（同じ配置でも毎回違う街になる）
4. **鉄道趣味との親和性**（実在車両を意識した外部モデル、リアルな運行）
5. **経営のバランス感覚**（投資と回収のタイミング）

### 6.3 絶対に外してはいけない要素
- ✅ 列車が**実際に線路上を走る**姿が見える
- ✅ 駅周辺に**街が自然に発展**する
- ✅ **昼夜サイクル**で街の表情が変わる
- ✅ **車窓モード**で自分の街を列車から眺められる
- ✅ **経営判断**が街の成長に影響する
- ✅ **資材の輸送**が発展の鍵になる

### 6.4 ブラウザゲームとしての差別化
- **即座にプレイ可能**（インストール不要）
- **マップ共有**（URLでシェア、5.4節）
- **軽量だが美しい**（Kenney/KayKitアセット + リアルライティング）

---

## 7. 参考資料

### A列車で行こうシリーズ
- **A列車で行こう9** (2010-現在): PC版の最新作。325種類の車両、約500種類の建物
- **A列車で行こう9 Evolution** (2026.6.4予定): Switch 2向け。ライティング・反射表現を再構築
- **A列車で行こう はじまる観光計画** (2021): 観光要素追加、カジュアル寄り

### 公式サイト
- https://www.atrain.jp/ — A列車ポータル
- https://www.atrain.jp/title/a9v5dx/ — A9 Version5.0
- https://www.atrain.jp/title/a9evo/ — A9 Evolution

### 特徴的なシステム（A9の詳細、参考・出典）
- **時間拡張**: 3倍〜450倍（ゲーム速度とは別に時計の進み方を変更）
- **転車台・機回し**: V5.0で追加（蒸気機関車のリアルな運行）
- **連結・増解結**: V5.0で追加（11両以上の編成を実現）
- **プロジェクト**: 国際空港、新幹線駅誘致、リニア駅誘致、鉄道博物館、ゴルフ場
- **コンストラクションモード**: 資金制限なしで自由に街を作れるモード
- **Steamワークショップ**: ユーザー作成シナリオの共有
